import { artist } from './artist'
import { artwork } from './artwork'
import { article } from './article'
import { settings } from './settings'

export const schemaTypes = [artist, artwork, article, settings]
